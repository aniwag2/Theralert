# ==============================================================================
# Theralert Application Environment Variables
#
# This file serves as an example for your .env file.
# Copy this to .env and fill in the values for your self-hosted deployment.
#
# DO NOT COMMIT YOUR .env FILE TO VERSION CONTROL!
# ==============================================================================

# --- NextAuth.js Configuration ------------------------------------------------

# NEXTAUTH_SECRET:
# A secret used by NextAuth.js to encrypt tokens and hash email verification hashes.
# This MUST be a strong, randomly generated string for production security.
#
# You can generate one using:
#   - Linux/macOS: openssl rand -base64 32
#   - Online: https://generate-secret.vercel.app/32 (use the 32-character hex option if possible)
NEXTAUTH_SECRET=your_super_secret_random_string_here

# NEXTAUTH_URL:
# The base URL where your Next.js application will be publicly accessible.
#
# For local development/testing: http://localhost:3000
# For deployment via Cloudflare Tunnels: https://your-subdomain.your-domain.com
# Example: https://theralert.example.com
NEXTAUTH_URL=http://localhost:3000

# --- Database Configuration (for the containerized MySQL service) -------------

# DB_NAME: The name of the database that will be created inside the MySQL container.
DB_NAME=patient_log

# DB_USER: The username for accessing the database.
DB_USER=theralert_user

# DB_PASSWORD: The password for the database user.
# IMPORTANT: Use a strong, unique password for production!
DB_PASSWORD=your_strong_db_password

# DATABASE_URL: The full connection string for your ORM (e.g., Prisma).
#
# - The 'db' hostname here refers to the 'db' service name in your docker-compose.yml.
# - '3306' is the default port for MySQL.
#
# If using Prisma with MySQL: "mysql://${DB_USER}:${DB_PASSWORD}@db:3306/${DB_NAME}"
DATABASE_URL="mysql://${DB_USER}:${DB_PASSWORD}@db:3306/${DB_NAME}"


# --- Email Configuration (if your app sends emails) -------------------------
# These are typically for services like password reset, new user verification, etc.
#
# EMAIL_USER: The username (email address) for your SMTP service.
EMAIL_USER=theralert@gmail.com

# EMAIL_PASS: The password or app-specific password for your SMTP service.
# If using Gmail, you might need to generate an App Password.
EMAIL_PASS=your_email_password_or_app_password

# EMAIL_SERVER: The SMTP server details.
# Examples:
#   - Gmail: smtp.gmail.com:587 (or smtps://user@gmail.com:password@smtp.gmail.com:465)
#   - SendGrid: smtp.sendgrid.net:587
#   - For more complex setups, you might need more specific details (e.g., STARTTLS, SSL).
# EMAIL_SERVER=smtp.gmail.com:587
# Or for direct SMTPs (SSL/TLS):
# EMAIL_SERVER=smtps://your_email_user:your_email_password@smtp.gmail.com:465


# --- Other Application-Specific Environment Variables -------------------------
# Add any other variables your Next.js app or API routes use.
# For example, API keys for external services, feature flags, etc.
#
# EXAMPLE_EXTERNAL_API_KEY=your_external_service_api_key
# FEATURE_FLAG_NEW_DASHBOARD=true